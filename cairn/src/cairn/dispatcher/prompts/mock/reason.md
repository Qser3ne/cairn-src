{
  "phase": "reason",
  "fact_ids": {fact_ids},
  "open_intents": {open_intents},
  "max_intents": {max_intents},
  "required_intent_fields": ["from", "description", "session_lock"],
  "session_lock_rule": "Use true for login state, cookies, tokens, account state, authentication flows, shared sessions, or global authentication context changes; otherwise use false."
}
